# Local config with real device IPs / community strings — never commit.
config.yaml

# Runtime logs
logs/

# Evidencia de PoCs contra paneles reales (contiene IPs, community strings
# y respuestas SNMP de hardware productivo). Nunca commitear.
evidence/

# Python
__pycache__/
*.pyc
.venv/

# Setuptools / pip editable-install metadata
*.egg-info/
build/
dist/

# pytest
.pytest_cache/

# macOS Finder metadata
.DS_Store
